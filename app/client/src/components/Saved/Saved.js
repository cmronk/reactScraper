import React from "react";
import "./Saved.css";

const Saved = props => (
    <div className="card">
        <div className="card-header">
        Saved
        </div>
        
    </div>
)

export default Saved;
